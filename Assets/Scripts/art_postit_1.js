




var myStyle: GUIStyle;    
var stringToEdit : String = "";
//var art_posit: Transform
/*var canType : boolean = false;


   function Update() {
        if (Input.GetMouseButtonDown(0)) {
            var hit: RaycastHit;
            var ray = Camera.main.ScreenPointToRay(Input.mousePosition);
            
            if (Physics.Raycast(ray, hit)) {
                if (hit.collider.gameObject.tag == "posit"){
                    canType = true;
            }
        }
    }
}


function OnGUI () {
			if(canType){
                       //GUI.skin.settings.cursorColor = Color.black;
        				// Make a text field that modifies stringToEdit.
        				//stringToEdit = GUI.TextField (Rect (Screen.width/4, Screen.height/4, 300, 300), stringToEdit, 200, myStyle);
				print("Can Type SHould be rockin");
            }
        }*/
    